#include <unordered_map>

static bool longestPatternSearch(const char *key, uint32_t key_size, int prev_pos_id)
{

  // traverse each char
  

  // 
  // If prev POS id is provided, consider it in the traverseal,
  // 0 = BOS(= no prev POS id)
  // 
  if (!prev_pos_id) {
    return n;
  }


}

int main(int argc, char **argv) {

  // concat 
  return 0;
}
